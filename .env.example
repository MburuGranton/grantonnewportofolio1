# Contentful API Configuration
# Create a .env file with these keys filled in with your Contentful credentials

# Required: Space ID from Contentful
VITE_CONTENTFUL_SPACE_ID=your_space_id_here

# Required: Content Delivery API access token
VITE_CONTENTFUL_ACCESS_TOKEN=your_access_token_here

# Optional: Set to 'true' to enable preview mode for draft content
# VITE_CONTENTFUL_PREVIEW=true

# Optional: Preview API access token (only needed if CONTENTFUL_PREVIEW=true)
# VITE_CONTENTFUL_PREVIEW_TOKEN=your_preview_token_here